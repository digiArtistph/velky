CREATE TABLE settings(
set_id INT NOT NULL AUTO_INCREMENT,
setting VARCHAR(255) NULL,
`value` VARCHAR(255) NULL,
PRIMARY KEY (set_id)) engine = INNODB COMMENT 'this is where we store our system settings';