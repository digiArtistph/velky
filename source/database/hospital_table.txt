CREATE TABLE hospitals(
h_id INT NOT NULL AUTO_INCREMENT,
name VARCHAR(255) NOT NULL,
address VARCHAR(255) NULL,
phone VARCHAR(150) NULL,
status ENUM("0", "1") DEFAULT "1",
PRIMARY KEY (h_id), UNIQUE INDEX (name))ENGINE=INNODB;